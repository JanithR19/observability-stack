receivers:
  otlp:
    protocols:
      grpc:
        endpoint: 0.0.0.0:4317
      http:
        endpoint: 0.0.0.0:4318

processors:
  batch:
  
  # Transform resource attributes for Prometheus labels
  resource:
    attributes:
      - key: job
        action: delete
      - key: service
        action: upsert
        from_attribute: service.name

exporters:
  # Tempo for traces via HTTP
  otlphttp/tempo:
    endpoint: ${env:TEMPO_ENDPOINT:-http://tempo:4318/v1/traces}
    tls:
      insecure: true
  
  # Prometheus for metrics
  prometheusremotewrite:
    endpoint: ${env:PROMETHEUS_ENDPOINT:-http://prometheus:9090/api/v1/write}
    resource_to_telemetry_conversion:
      enabled: true
  
  # Loki for logs via OTLP/HTTP to the standard Loki OTLP endpoint
  otlphttp/loki:
    endpoint: ${env:LOKI_ENDPOINT:-http://loki:3100/otlp/v1/logs}
    tls:
      insecure: true

service:
  telemetry:
    metrics:
      address: 0.0.0.0:8888
  pipelines:
    traces:
      receivers: [otlp]
      processors: [batch]
      exporters: [otlphttp/tempo]
    metrics:
      receivers: [otlp]
      processors: [batch, resource]
      exporters: [prometheusremotewrite]
    logs:
      receivers: [otlp]
      processors: [batch]
      exporters: [otlphttp/loki]